+ screen_size is min of window width/height - 1
+ create newwin of that size
+ draw box around newwin
+ make timer for each game tick
+ store score in a variable
+ use alternate chars to use as different layers (top-down view)
+ first layer should be drawn already
- layers with one symbol come in from the top and bounce off the bottom/top, other symbol starts left, bounces right/left
- space/enter to drop current box on tower